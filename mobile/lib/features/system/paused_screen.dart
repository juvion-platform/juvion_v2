import 'package:flutter/material.dart';

/// Placeholder — replaced when the system-states task lands.
class PausedScreen extends StatelessWidget {
  const PausedScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('/paused')));
}
