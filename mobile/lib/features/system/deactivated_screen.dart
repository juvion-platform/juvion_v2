import 'package:flutter/material.dart';

/// Placeholder — replaced when the system-states task lands.
class DeactivatedScreen extends StatelessWidget {
  const DeactivatedScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('/deactivated')));
}
