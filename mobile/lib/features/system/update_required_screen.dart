import 'package:flutter/material.dart';

/// Placeholder — replaced when the system-states task lands.
class UpdateRequiredScreen extends StatelessWidget {
  const UpdateRequiredScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('/update-required')));
}
