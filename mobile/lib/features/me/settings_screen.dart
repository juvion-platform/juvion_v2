import 'package:flutter/material.dart';

/// Placeholder — replaced by the Me task.
class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('/me/settings')));
}
