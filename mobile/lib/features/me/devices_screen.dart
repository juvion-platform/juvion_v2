import 'package:flutter/material.dart';

/// Placeholder — replaced by the Me task.
class DevicesScreen extends StatelessWidget {
  const DevicesScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('/me/devices')));
}
