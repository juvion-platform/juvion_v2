import 'package:flutter/material.dart';

/// Placeholder — replaced by the Me task.
class MeScreen extends StatelessWidget {
  const MeScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('/me')));
}
