import 'package:flutter/material.dart';

/// Placeholder — replaced by the Me task.
class ChangePasswordScreen extends StatelessWidget {
  const ChangePasswordScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('/me/change-password')));
}
