import 'package:flutter/material.dart';

/// Placeholder — replaced by the sign-in task.
class SetPasswordScreen extends StatelessWidget {
  const SetPasswordScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('/set-password')));
}
