import 'package:flutter/material.dart';

/// Placeholder — replaced by the Today task.
class TodayShellScreen extends StatelessWidget {
  const TodayShellScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('/today')));
}
