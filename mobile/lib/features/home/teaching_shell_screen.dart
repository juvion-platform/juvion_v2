import 'package:flutter/material.dart';

/// Placeholder — replaced by the Teaching task.
class TeachingShellScreen extends StatelessWidget {
  const TeachingShellScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('/teaching')));
}
