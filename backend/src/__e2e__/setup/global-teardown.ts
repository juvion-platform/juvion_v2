// Teardown handled by global-setup.ts teardown export.
// This file exists as a placeholder if separate teardown is needed later.
export async function teardown() {
  // MongoMemoryServer stopped by global-setup teardown
}
