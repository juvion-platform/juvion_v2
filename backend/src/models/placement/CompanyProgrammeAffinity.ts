import { Schema, model, Document } from 'mongoose';
export interface ICompanyProgrammeAffinity extends Document { collegeId: Schema.Types.ObjectId; companyId: Schema.Types.ObjectId; programmeId: Schema.Types.ObjectId; placementSeasonId?: Schema.Types.ObjectId; historicalHires: number; avgCtcLpa: number; conversionRate: number; programmeFitScore: number; lastUpdated: Date; }
const schema = new Schema<ICompanyProgrammeAffinity>({ collegeId: { type: Schema.Types.ObjectId, required: true, index: true }, companyId: { type: Schema.Types.ObjectId, ref: 'Company', required: true }, programmeId: { type: Schema.Types.ObjectId, ref: 'Programme', required: true }, placementSeasonId: { type: Schema.Types.ObjectId, ref: 'PlacementSeason' }, historicalHires: { type: Number, default: 0 }, avgCtcLpa: { type: Number, default: 0 }, conversionRate: { type: Number, default: 0 }, programmeFitScore: { type: Number, default: 0 }, lastUpdated: { type: Date, default: Date.now } }, { timestamps: true });
schema.index({ collegeId: 1, companyId: 1, programmeId: 1 }, { unique: true });
export const CompanyProgrammeAffinity = model<ICompanyProgrammeAffinity>('CompanyProgrammeAffinity', schema);
