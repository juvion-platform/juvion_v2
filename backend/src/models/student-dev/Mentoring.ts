import { Schema, model, Document } from 'mongoose';
export interface IMentoring extends Document { collegeId: Schema.Types.ObjectId; mentorId: Schema.Types.ObjectId; menteeId: Schema.Types.ObjectId; academicYearId: Schema.Types.ObjectId; meetingDate?: Date; notes?: string; status: string; closeReason?: string; closedAt?: Date; }
const schema = new Schema<IMentoring>({ collegeId: { type: Schema.Types.ObjectId, required: true, index: true }, mentorId: { type: Schema.Types.ObjectId, ref: 'Faculty', required: true }, menteeId: { type: Schema.Types.ObjectId, ref: 'Student', required: true }, academicYearId: { type: Schema.Types.ObjectId, ref: 'AcademicYear', required: true }, meetingDate: Date, notes: String, status: { type: String, enum: ['active', 'completed', 'closed'], default: 'active' }, closeReason: { type: String, enum: ['exit_graduation', 'exit_withdrawal', 'exit_expulsion', 'exit_dropout', 'exit_transfer', 'reassignment', 'academic_year_end'] }, closedAt: Date }, { timestamps: true });
schema.index({ collegeId: 1, mentorId: 1, menteeId: 1 });
export const Mentoring = model<IMentoring>('Mentoring', schema);
