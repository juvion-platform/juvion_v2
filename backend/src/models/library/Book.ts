import { Schema, model, Document } from 'mongoose';
export interface IBook extends Document { collegeId: Schema.Types.ObjectId; isbn?: string; title: string; author: string; publisher?: string; edition?: string; year?: number; category: string; departmentId?: Schema.Types.ObjectId; totalCopies: number; availableCopies: number; location: string; }
const schema = new Schema<IBook>({ collegeId: { type: Schema.Types.ObjectId, required: true, index: true }, isbn: String, title: { type: String, required: true }, author: { type: String, required: true }, publisher: String, edition: String, year: Number, category: { type: String, enum: ['textbook', 'reference', 'journal', 'magazine', 'thesis', 'general', 'digital'], required: true }, departmentId: { type: Schema.Types.ObjectId, ref: 'Department' }, totalCopies: { type: Number, default: 1 }, availableCopies: { type: Number, default: 1 }, location: String }, { timestamps: true });
schema.index({ collegeId: 1, title: 'text', author: 'text' });
export const Book = model<IBook>('Book', schema);
