import { Schema, model, Document } from 'mongoose';
export interface IRoomBooking extends Document { collegeId: Schema.Types.ObjectId; roomId: Schema.Types.ObjectId; bookedBy: Schema.Types.ObjectId; date: Date; startTime: string; endTime: string; purpose: string; status: string; requesterModule?: string; approvalChain?: { approverId?: Schema.Types.ObjectId; approverRole?: string; status?: string; approvedAt?: Date }[]; attendeeCount?: number; facilityUsageLogged: boolean; noShow: boolean; }
const schema = new Schema<IRoomBooking>({ collegeId: { type: Schema.Types.ObjectId, required: true, index: true }, roomId: { type: Schema.Types.ObjectId, ref: 'Room', required: true }, bookedBy: { type: Schema.Types.ObjectId, ref: 'Person', required: true }, date: { type: Date, required: true }, startTime: { type: String, required: true }, endTime: { type: String, required: true }, purpose: { type: String, required: true }, status: { type: String, enum: ['pending', 'pending_approval', 'approved', 'confirmed', 'active', 'completed', 'rejected', 'cancelled', 'no_show'], default: 'pending' }, requesterModule: String, approvalChain: [new Schema({ approverId: Schema.Types.ObjectId, approverRole: String, status: String, approvedAt: Date }, { _id: false })], attendeeCount: Number, facilityUsageLogged: { type: Boolean, default: false }, noShow: { type: Boolean, default: false } }, { timestamps: true });
schema.index({ collegeId: 1, roomId: 1, date: 1 });
export const RoomBooking = model<IRoomBooking>('RoomBooking', schema);
