import { Schema, model, Document } from 'mongoose';
export interface IAssessmentRubric extends Document { collegeId: Schema.Types.ObjectId; criterionId: Schema.Types.ObjectId; bodyId: Schema.Types.ObjectId; gradeDescriptors: { grade: string; minScore: number; maxScore: number; description: string }[]; scoringMethod: string; maxScore: number; weightageInOverall: number; version: string; isActive: boolean; }
const schema = new Schema<IAssessmentRubric>({ collegeId: { type: Schema.Types.ObjectId, required: true, index: true }, criterionId: { type: Schema.Types.ObjectId, ref: 'ComplianceCriteria', required: true }, bodyId: { type: Schema.Types.ObjectId, ref: 'AccreditationBody', required: true }, gradeDescriptors: [{ grade: { type: String, required: true }, minScore: { type: Number, required: true }, maxScore: { type: Number, required: true }, description: { type: String, required: true } }], scoringMethod: { type: String, enum: ['quantitative', 'qualitative', 'mixed'], required: true }, maxScore: { type: Number, required: true }, weightageInOverall: { type: Number, required: true }, version: { type: String, required: true }, isActive: { type: Boolean, default: true } }, { timestamps: true });
schema.index({ collegeId: 1, bodyId: 1, criterionId: 1 });
export const AssessmentRubric = model<IAssessmentRubric>('AssessmentRubric', schema);
