# Completion: Task L8 — API reference + QA/deploy checklist

**Feature:** llm-spend-limits
**Completed:** 2026-04-28
**Person:** srinikandula
**Final Status:** Done

## Files Changed

- **Created:** `backend/docs/api/llm-spend-limits.md` — feature reference (concepts, schema, endpoints, gate flow, banner UX, cron, rollback, deviations, open questions)
- **Created:** `backend/docs/api/llm-spend-limits-qa-checklist.md` — verifiable boolean checklist for Admin / SRE / Product handoff

## Verification

- Markdown lints clean (no broken links — every `./l*.md` reference points at an existing file).
- All commands in the QA checklist match the actual implementation (verified against `backend/src/workers/llm-usage-weekly.worker.ts`, `backend/src/modules/colleges/{routes,controller,validation}.ts`, `backend/src/modules/platform/spend-limits/`, `admin-portal/src/components/finance/BudgetBanner.tsx`).
- Cross-referenced with `spec.md`, `plan.md`, and tasks.md for fidelity.
- Mirrored the structure of the existing `fee-analytics-ai-native.md` / `fee-analytics-ai-native-qa-checklist.md` pair so the docs feel consistent with the rest of the API surface.

## AC Coverage (from tasks.md §L8)

API reference (`llm-spend-limits.md`):
- ✓ Concepts (rolling 7-day window, soft alert vs hard block, bypass on `weeklyInr=0`)
- ✓ College.aiSpendLimits field shape
- ✓ LLMUsageSnapshot collection
- ✓ Pre-call gate flow (where it fires, the SpendCheckResult shape, the 429 body shape, the cache layer)
- ✓ 1 new endpoint documented (`PATCH /api/colleges/:id/ai-spend-limits`)
- ✓ Frontend banner UX (states, copy, degraded mode, admin UI section)
- ✓ Weekly cron schedule (`0 6 * * 1` UTC, concurrency 10, retries 3, snapshot shape, error tolerance)
- ✓ Open questions (timezone, header surface, fail-open vs fail-closed, soft vs hard cap UX)

QA / deploy checklist (`llm-spend-limits-qa-checklist.md`):
- ✓ §0 Prerequisites (8 tasks Done; backend ≥885 unit + ≥12 e2e; admin ≥57; typecheck 0 errors)
- ✓ §1 Schema verification (College.aiSpendLimits + LLMUsageSnapshot, with mongo-shell one-liners)
- ✓ §2 Default behavior verification (no regression for tenants with `weeklyInr=0`)
- ✓ §3 80% banner test (set tight limit, drive cost, verify amber banner + log line)
- ✓ §4 100% block test (verify 429 + structured detail + degraded mode across all 4 AI surfaces)
- ✓ §5 Admin override test (PATCH bumps limit; cache invalidates; AuditLog row)
- ✓ §6 Weekly cron verification (BullMQ key, cronPattern, snapshot rows, inactive-skip, log lines)
- ✓ §7 Rollback plan (disable globally via `weeklyInr=0`, disable cron, revert commits — with safety guarantees)
- ✓ §8 Sign-off (Admin, SRE, Product)
- ✓ Appendix with useful one-liners

## Spec Gaps Surfaced

None new. The doc surfaces the four already-known deviations from earlier tasks:

1. Array endpoints (risk-scores, situations, reminder-drafts) lack body `budgetWarning` — workaround documented; future header-based fix described.
2. Streaming chat 429 lands as SSE error event, not 429 status code — design intent documented.
3. AI Spend Limits section is edit-only (create flow doesn't show it) — documented.
4. No per-run audit table for cron failures — flagged for future observability.

## Notes

- Mirrored `fee-analytics-ai-native.md` structure so future readers can navigate both docs the same way.
- Every command in the QA checklist is a copy-paste line — no "fill in <X>" placeholders for the SRE-facing checks.
- The §7 rollback plan explicitly notes that the gate is the ONLY new write path, so disabling cannot corrupt existing data.
- §6 cron verification leans on observable signals (BullMQ keys, log line counts, snapshot row counts) rather than requiring access to the worker process — runs on dev or prod.

## Feature Complete

All 8 tasks (L1-L8) are now `Done`. The full `llm-spend-limits` feature ships with:

- 83 backend tests + 12 e2e + 14 frontend unit tests
- 0 TypeScript errors across both workspaces
- Clean Vite build for the admin portal
- Full deployment + rollback playbook
