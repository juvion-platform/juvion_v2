# E2E Testing Infrastructure — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an E2E test suite that validates all API endpoints and 6 critical business workflows against an in-memory MongoDB, with role-based request helpers and isolated test data.

**Architecture:** Vitest + Supertest + mongodb-memory-server. Each test file seeds its own data via factories, runs HTTP assertions against the real Express app, then drops all collections. Sequential execution (`singleFork`) because workflow tests depend on ordered steps within a suite.

**Tech Stack:** Vitest 4, Supertest, mongodb-memory-server, jsonwebtoken (existing), bcryptjs (existing)

**Spec:** `docs/superpowers/specs/2026-04-12-e2e-testing-design.md`

---

### Task 1: Refactor app.ts for testability + install dependencies

Currently `app.ts` calls `connectDB()` and `app.listen()` as a side effect on import. This prevents clean test bootstrapping. We need to separate the Express app creation from the server startup.

**Files:**
- Modify: `backend/src/app.ts` — remove `start()`, `connectDB` import, and `dotenv/config` import
- Create: `backend/src/server.ts` — new entry point that connects DB + starts listening
- Modify: `backend/package.json` — update scripts to use `server.ts`, add test deps + scripts
- Create: `backend/vitest.e2e.config.ts` — E2E-specific vitest configuration

- [ ] **Step 1: Create `backend/src/server.ts`**

This becomes the new entry point for running the server (dev + prod).

```typescript
// backend/src/server.ts
import 'dotenv/config';
import { connectDB } from './config/db';
import app from './app';

const PORT = process.env.PORT || 3003;

async function start() {
  await connectDB();
  app.listen(PORT, () => {
    console.log(`Juvion v2 API running on http://localhost:${PORT}`);
  });
}

start().catch((err) => {
  console.error('Failed to start:', err);
  process.exit(1);
});
```

- [ ] **Step 2: Strip side effects from `backend/src/app.ts`**

Remove the `dotenv/config` import, `connectDB` import, `PORT` constant, `start()` function, and `start().catch()` call. Keep everything else (Express app creation, middleware, routes, `export default app`).

The file should end with just `export default app;` — no calls to `connectDB()` or `app.listen()`.

After edit, `app.ts` should look like:

```typescript
import express from 'express';
import cors from 'cors';
import helmet from 'helmet';
import morgan from 'morgan';
import rateLimit from 'express-rate-limit';
import { errorHandler } from './middleware/errorHandler';
import apiRouter from './routes';
import authRouter from './modules/auth/routes';

// Register workflow definitions
import './shared/workflow/definitions';
import './modules/admissions/workflow.handlers';

const app = express();

app.use(helmet());

const allowedOrigins = (process.env.ALLOWED_ORIGINS || process.env.CLIENT_URL || 'http://localhost:5173')
  .split(',')
  .map(o => o.trim())
  .filter(Boolean);

app.use(cors({
  origin(origin, callback) {
    if (!origin) return callback(null, true);
    if (allowedOrigins.includes(origin)) return callback(null, true);
    callback(new Error(`Origin ${origin} not allowed by CORS`));
  },
  credentials: true,
}));
app.use(morgan(process.env.NODE_ENV === 'production' ? 'combined' : 'dev'));
app.use(express.json({ limit: '10mb' }));

if (process.env.NODE_ENV === 'production' && (!process.env.JWT_SECRET || process.env.JWT_SECRET === 'dev-secret')) {
  console.error('FATAL: JWT_SECRET must be set to a secure value in production');
  process.exit(1);
}

app.use(rateLimit({ windowMs: 60_000, max: 100, standardHeaders: true, legacyHeaders: false }));
app.use('/api/auth/login', rateLimit({ windowMs: 15 * 60_000, max: 10, message: { error: 'Too many login attempts. Try again in 15 minutes.' } }));

app.get('/api/health', (_req, res) => res.json({ status: 'ok', version: '2.0.0' }));
app.use('/api/auth', authRouter);
app.use('/api', apiRouter);
app.use(errorHandler);

export default app;
```

- [ ] **Step 3: Update `backend/package.json` scripts**

Change `dev` and `start` scripts to point to `server.ts`:

```json
{
  "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
  "start": "node dist/server.js"
}
```

- [ ] **Step 4: Install E2E test dependencies**

```bash
cd backend && npm install -D supertest @types/supertest mongodb-memory-server
```

- [ ] **Step 5: Add E2E test scripts to `backend/package.json`**

Add these to the `scripts` section:

```json
{
  "test:e2e": "vitest run --config vitest.e2e.config.ts",
  "test:all": "vitest run && vitest run --config vitest.e2e.config.ts"
}
```

- [ ] **Step 6: Create `backend/vitest.e2e.config.ts`**

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['src/__e2e__/**/*.test.ts'],
    testTimeout: 30_000,
    hookTimeout: 30_000,
    globalSetup: ['src/__e2e__/setup/global-setup.ts'],
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } },
  },
});
```

- [ ] **Step 7: Verify the refactoring works**

Run: `cd backend && npm run typecheck`

Expected: zero TypeScript errors. The `server.ts` entry point should compile, and `app.ts` should still export the Express app.

- [ ] **Step 8: Commit**

```bash
git add backend/src/server.ts backend/src/app.ts backend/package.json backend/vitest.e2e.config.ts
git commit -m "refactor: separate server startup from app creation for testability

Extract connectDB + listen into server.ts. app.ts now exports a pure
Express app with no side effects. Add supertest, mongodb-memory-server,
vitest E2E config, and test:e2e npm script."
```

---

### Task 2: Global setup/teardown + test app bootstrapper

**Files:**
- Create: `backend/src/__e2e__/setup/global-setup.ts`
- Create: `backend/src/__e2e__/setup/global-teardown.ts`
- Create: `backend/src/__e2e__/setup/test-app.ts`

- [ ] **Step 1: Create `backend/src/__e2e__/setup/global-setup.ts`**

```typescript
import { MongoMemoryServer } from 'mongodb-memory-server';

let mongod: MongoMemoryServer;

export async function setup() {
  mongod = await MongoMemoryServer.create({
    binary: { version: '7.0.0' },
  });
  process.env.MONGO_TEST_URI = mongod.getUri();
}

export async function teardown() {
  if (mongod) {
    await mongod.stop();
  }
}
```

- [ ] **Step 2: Create `backend/src/__e2e__/setup/global-teardown.ts`**

```typescript
// Teardown handled by global-setup.ts teardown export.
// This file exists as a placeholder if separate teardown is needed later.
export async function teardown() {
  // MongoMemoryServer stopped by global-setup teardown
}
```

- [ ] **Step 3: Create `backend/src/__e2e__/setup/test-app.ts`**

```typescript
import mongoose from 'mongoose';
import type { Express } from 'express';

let app: Express | null = null;

/**
 * Returns the Express app connected to the MongoMemoryServer.
 * Call in beforeAll of each test file.
 */
export async function getTestApp(): Promise<Express> {
  if (!app) {
    // Set env vars before importing app (app reads JWT_SECRET, NODE_ENV)
    process.env.JWT_SECRET = 'test-secret';
    process.env.NODE_ENV = 'test';
    process.env.RBAC_ENFORCE = 'false';

    const mod = await import('../../app');
    app = mod.default;
  }

  // Connect mongoose if not already connected
  if (mongoose.connection.readyState === 0) {
    const uri = process.env.MONGO_TEST_URI;
    if (!uri) throw new Error('MONGO_TEST_URI not set. Did global-setup run?');
    await mongoose.connect(uri);
  }

  return app;
}

/**
 * Drop all collections and disconnect. Call in afterAll of each test file.
 */
export async function cleanupTestApp(): Promise<void> {
  const collections = await mongoose.connection.db!.collections();
  for (const collection of collections) {
    await collection.deleteMany({});
  }
}
```

- [ ] **Step 4: Verify setup compiles**

Run: `cd backend && npx tsc --noEmit`

Expected: zero errors.

- [ ] **Step 5: Commit**

```bash
git add backend/src/__e2e__/setup/
git commit -m "test: add E2E global setup/teardown and test-app bootstrapper

MongoMemoryServer starts once per vitest run. test-app.ts lazily imports
the Express app and connects mongoose to the in-memory DB. cleanupTestApp
drops all documents between test files."
```

---

### Task 3: User factory + auth token helper

**Files:**
- Create: `backend/src/__e2e__/factories/user.factory.ts`

**Context:**
- User model at `backend/src/models/User.ts` — fields: `collegeId?, email, password, name, role, personaType, personId?, isActive`
- JWT payload: `{ id, name, email, role, personaType, collegeId? }`
- Auth middleware verifies with `process.env.JWT_SECRET || 'dev-secret'` — test-app sets it to `'test-secret'`
- Password hashing: bcryptjs

- [ ] **Step 1: Create `backend/src/__e2e__/factories/user.factory.ts`**

```typescript
import bcrypt from 'bcryptjs';
import jwt from 'jsonwebtoken';
import User from '../../models/User';

const JWT_SECRET = 'test-secret';

interface CreateUserOpts {
  collegeId?: string;
  role: string;
  personaType: string;
  name: string;
  email: string;
  password?: string;
  personId?: string;
}

interface TestUser {
  user: InstanceType<typeof User>;
  token: string;
}

/**
 * Create a User document in the test DB and return it with a pre-signed JWT.
 */
export async function createTestUser(opts: CreateUserOpts): Promise<TestUser> {
  const password = opts.password ?? 'test123';
  const hashed = await bcrypt.hash(password, 4); // low rounds for speed

  const user = await User.create({
    collegeId: opts.collegeId,
    email: opts.email,
    password: hashed,
    name: opts.name,
    role: opts.role,
    personaType: opts.personaType,
    personId: opts.personId,
    isActive: true,
  });

  const token = createAuthToken({
    id: String(user._id),
    name: user.name,
    email: user.email,
    role: user.role,
    personaType: user.personaType,
    collegeId: opts.collegeId,
  });

  return { user, token };
}

/**
 * Generate a JWT without touching the DB.
 * Use when you already have a user and just need a token.
 */
export function createAuthToken(payload: {
  id: string;
  name: string;
  email: string;
  role: string;
  personaType: string;
  collegeId?: string;
}): string {
  return jwt.sign(payload, JWT_SECRET, { expiresIn: '1h' });
}
```

- [ ] **Step 2: Verify compilation**

Run: `cd backend && npx tsc --noEmit`

Expected: zero errors.

- [ ] **Step 3: Commit**

```bash
git add backend/src/__e2e__/factories/user.factory.ts
git commit -m "test: add user factory and auth token helper for E2E tests"
```

---

### Task 4: Base seed data

**Files:**
- Create: `backend/src/__e2e__/setup/seed-base.ts`

**Context:**
- College model: `name, code, address: { line1, city, state, pincode }, contactEmail, contactPhone, subscription: { plan, status }, status`
- Department: `collegeId, code, name, hodId?, isActive`
- Regulation: `collegeId, code, name, effectiveFromYear, totalCredits, maxYears, isActive`
- Programme: `collegeId, code, name, level, durationYears, regulationId, isActive`
- Branch: `collegeId, code, name, programmeId, departmentId?, intake, isActive`
- AcademicYear: `collegeId, code, label, startDate, endDate, isCurrent`
- Semester: `collegeId, academicYearId, number, year, startDate, endDate, status`
- Section: `collegeId, name, branchId, batchId, year, semester, capacity`
- Batch: `collegeId, code, name, admissionYear, programmeId, regulationId, isActive`
- RBAC Policy model at `backend/src/models/platform/Policy.ts`
- Default policies in `backend/src/shared/rbac/defaults.ts`

- [ ] **Step 1: Create `backend/src/__e2e__/setup/seed-base.ts`**

```typescript
import { College } from '../../models/College';
import {
  AcademicYear, Batch, Branch, Department, Programme,
  Regulation, Section, Semester,
} from '../../models';
import { Policy } from '../../models/platform/Policy';
import { DEFAULT_POLICIES } from '../../shared/rbac/defaults';
import { createTestUser } from '../factories/user.factory';

export interface BaseFixtures {
  collegeId: string;
  college: InstanceType<typeof College>;
  regulation: InstanceType<typeof Regulation>;
  cse: InstanceType<typeof Department>;
  ece: InstanceType<typeof Department>;
  btech: InstanceType<typeof Programme>;
  cseBranch: InstanceType<typeof Branch>;
  eceBranch: InstanceType<typeof Branch>;
  batch: InstanceType<typeof Batch>;
  cseSection: InstanceType<typeof Section>;
  ay: InstanceType<typeof AcademicYear>;
  sem1: InstanceType<typeof Semester>;
  sem2: InstanceType<typeof Semester>;
  superAdmin: { user: any; token: string };
  admin: { user: any; token: string };
  principal: { user: any; token: string };
}

/**
 * Seed structural entities needed by almost every test.
 * Call in beforeAll of each test file.
 */
export async function seedBase(): Promise<BaseFixtures> {
  // 1. College
  const college = await College.create({
    name: 'JIT Test College',
    code: 'JIT-TEST',
    address: { line1: '1 Test Road', city: 'Hyderabad', state: 'Telangana', pincode: '500001' },
    contactEmail: 'admin@jit-test.edu',
    contactPhone: '9000000001',
    subscription: { plan: 'premium', status: 'active' },
    status: 'active',
  });
  const collegeId = String(college._id);

  // 2. Regulation (required by Programme, Course, Batch)
  const regulation = await Regulation.create({
    collegeId, code: 'R20', name: 'R20 Regulation',
    effectiveFromYear: 2020, totalCredits: 160, maxYears: 6, isActive: true,
  });

  // 3. Departments
  const cse = await Department.create({ collegeId, code: 'CSE', name: 'Computer Science', isActive: true });
  const ece = await Department.create({ collegeId, code: 'ECE', name: 'Electronics', isActive: true });

  // 4. Programme + Branches
  const btech = await Programme.create({
    collegeId, code: 'BTECH', name: 'B.Tech', level: 'UG',
    durationYears: 4, regulationId: regulation._id, isActive: true,
  });
  const cseBranch = await Branch.create({
    collegeId, code: 'CSE', name: 'CSE', programmeId: btech._id,
    departmentId: cse._id, intake: 120, isActive: true,
  });
  const eceBranch = await Branch.create({
    collegeId, code: 'ECE', name: 'ECE', programmeId: btech._id,
    departmentId: ece._id, intake: 60, isActive: true,
  });

  // 5. Batch
  const batch = await Batch.create({
    collegeId, code: '2024', name: '2024 Batch', admissionYear: 2024,
    programmeId: btech._id, regulationId: regulation._id, isActive: true,
  });

  // 6. Academic Year + Semesters
  const ay = await AcademicYear.create({
    collegeId, code: '2024-25', label: '2024-25',
    startDate: new Date('2024-06-01'), endDate: new Date('2025-05-31'), isCurrent: true,
  });
  const sem1 = await Semester.create({
    collegeId, academicYearId: ay._id, number: 1, year: 1,
    startDate: new Date('2024-06-01'), endDate: new Date('2024-11-30'), status: 'active',
  });
  const sem2 = await Semester.create({
    collegeId, academicYearId: ay._id, number: 2, year: 1,
    startDate: new Date('2024-12-01'), endDate: new Date('2025-05-31'), status: 'upcoming',
  });

  // 7. Section (for course offerings / enrollment)
  const cseSection = await Section.create({
    collegeId, name: 'A', branchId: cseBranch._id, batchId: batch._id,
    year: 1, semester: 1, capacity: 60,
  });

  // 8. Users
  const superAdmin = await createTestUser({
    role: 'super_admin', personaType: 'L-SADM',
    name: 'Super Admin', email: 'super@test.com',
  });
  const admin = await createTestUser({
    collegeId, role: 'admin', personaType: 'L-ADMIN',
    name: 'College Admin', email: 'admin@test.com',
  });
  const principal = await createTestUser({
    collegeId, role: 'principal', personaType: 'L-PRIN',
    name: 'Principal', email: 'principal@test.com',
  });

  // 9. RBAC default policies
  await seedDefaultPolicies();

  return {
    college, collegeId, regulation, cse, ece, btech, cseBranch, eceBranch,
    batch, cseSection, ay, sem1, sem2, superAdmin, admin, principal,
  };
}

/**
 * Seed the 69 default RBAC policies from defaults.ts into the test DB.
 */
async function seedDefaultPolicies(): Promise<void> {
  const docs = DEFAULT_POLICIES.map((p) => ({
    ...p,
    createdBy: 'seed',
  }));
  await Policy.insertMany(docs);
}
```

- [ ] **Step 2: Verify compilation**

Run: `cd backend && npx tsc --noEmit`

Expected: zero errors.

- [ ] **Step 3: Commit**

```bash
git add backend/src/__e2e__/setup/seed-base.ts
git commit -m "test: add base seed data for E2E tests

Seeds college, regulation, departments, programme, branches, batch,
academic year, semesters, section, admin/principal/super-admin users,
and 69 default RBAC policies."
```

---

### Task 5: Request helper + assertion helpers

**Files:**
- Create: `backend/src/__e2e__/helpers/request.ts`
- Create: `backend/src/__e2e__/helpers/assertions.ts`

- [ ] **Step 1: Create `backend/src/__e2e__/helpers/request.ts`**

```typescript
import supertest from 'supertest';
import type { Express } from 'express';

/**
 * Wraps supertest with auth convenience methods.
 *
 * Usage:
 *   const api = createTestApi(app);
 *   await api.get('/api/health').expect(200);
 *   await api.as(adminToken).get('/api/people/students').expect(200);
 */
export function createTestApi(app: Express) {
  const agent = supertest(app);

  function withAuth(token: string) {
    return {
      get: (url: string) => agent.get(url).set('Authorization', `Bearer ${token}`),
      post: (url: string) => agent.post(url).set('Authorization', `Bearer ${token}`),
      put: (url: string) => agent.put(url).set('Authorization', `Bearer ${token}`),
      delete: (url: string) => agent.delete(url).set('Authorization', `Bearer ${token}`),
    };
  }

  return {
    get: (url: string) => agent.get(url),
    post: (url: string) => agent.post(url),
    put: (url: string) => agent.put(url),
    delete: (url: string) => agent.delete(url),
    as: withAuth,
  };
}

export type TestApi = ReturnType<typeof createTestApi>;
```

- [ ] **Step 2: Create `backend/src/__e2e__/helpers/assertions.ts`**

```typescript
/**
 * Assert that a response body has the standard paginated shape:
 * { items: T[], total: number, page: number, pages: number }
 */
export function expectPaginated(body: Record<string, unknown>, opts?: { minItems?: number }) {
  expect(body).toHaveProperty('items');
  expect(body).toHaveProperty('total');
  expect(body).toHaveProperty('page');
  expect(body).toHaveProperty('pages');
  expect(Array.isArray(body.items)).toBe(true);
  if (opts?.minItems !== undefined) {
    expect((body.items as unknown[]).length).toBeGreaterThanOrEqual(opts.minItems);
  }
}

/**
 * Assert a response body has an error field.
 */
export function expectError(body: Record<string, unknown>) {
  expect(body).toHaveProperty('error');
}

/**
 * Assert that a response body matches a created entity shape:
 * has _id, collegeId, createdAt, updatedAt.
 */
export function expectEntity(body: Record<string, unknown>) {
  expect(body).toHaveProperty('_id');
  expect(body).toHaveProperty('createdAt');
  expect(body).toHaveProperty('updatedAt');
}
```

- [ ] **Step 3: Verify compilation**

Run: `cd backend && npx tsc --noEmit`

Expected: zero errors.

- [ ] **Step 4: Commit**

```bash
git add backend/src/__e2e__/helpers/
git commit -m "test: add request wrapper and assertion helpers for E2E tests"
```

---

### Task 6: Entity factories — student, policy

**Files:**
- Create: `backend/src/__e2e__/factories/student.factory.ts`
- Create: `backend/src/__e2e__/factories/policy.factory.ts`

**Context:**
- Person model: `collegeId, name, phone, email?, gender?, dob?, address?`
- Student model: `collegeId, personId, admissionYear, programmeId?, branchId?, batchId?, rollNumber?, status, onboardingStatus, category?, quota?`
- RBAC Policy model at `backend/src/models/platform/Policy.ts`: `collegeId?, role, personaType?, module, action, effect, scope?, priority, description?, isActive, createdBy?`

- [ ] **Step 1: Create `backend/src/__e2e__/factories/student.factory.ts`**

```typescript
import { Person, Student } from '../../models';
import { createTestUser } from './user.factory';

let studentCounter = 0;

interface CreateStudentOpts {
  branchId?: string;
  batchId?: string;
  programmeId?: string;
  name?: string;
  email?: string;
  admissionYear?: number;
}

interface TestStudent {
  person: InstanceType<typeof Person>;
  student: InstanceType<typeof Student>;
  user: any;
  token: string;
}

/**
 * Create Person → Student → User chain in one call.
 * Returns all three documents plus a student-role JWT.
 */
export async function createTestStudent(collegeId: string, opts?: CreateStudentOpts): Promise<TestStudent> {
  studentCounter++;
  const name = opts?.name ?? `Test Student ${studentCounter}`;
  const email = opts?.email ?? `student${studentCounter}@test.com`;

  const person = await Person.create({
    collegeId,
    name,
    phone: `90000${String(studentCounter).padStart(5, '0')}`,
    email,
    gender: 'male',
  });

  const student = await Student.create({
    collegeId,
    personId: person._id,
    admissionYear: opts?.admissionYear ?? 2024,
    programmeId: opts?.programmeId,
    branchId: opts?.branchId,
    batchId: opts?.batchId,
    rollNumber: `24JIT${String(studentCounter).padStart(4, '0')}`,
    status: 'active',
    onboardingStatus: 'completed',
  });

  const { user, token } = await createTestUser({
    collegeId,
    role: 'student',
    personaType: 'L-STU',
    name,
    email,
    personId: String(person._id),
  });

  return { person, student, user, token };
}
```

- [ ] **Step 2: Create `backend/src/__e2e__/factories/policy.factory.ts`**

```typescript
import { Policy } from '../../models/platform/Policy';

interface CreatePolicyOpts {
  role: string;
  module: string;
  action: string;
  effect: 'allow' | 'deny';
  personaType?: string;
  scope?: { departmentOnly?: boolean; selfOnly?: boolean; subDomain?: string };
  priority?: number;
  description?: string;
}

/**
 * Create an RBAC policy for a specific college.
 * Used to test college-specific overrides of system defaults.
 */
export async function createTestPolicy(collegeId: string, opts: CreatePolicyOpts) {
  return Policy.create({
    collegeId,
    role: opts.role,
    personaType: opts.personaType ?? null,
    module: opts.module,
    action: opts.action,
    effect: opts.effect,
    scope: opts.scope,
    priority: opts.priority ?? 650,
    description: opts.description ?? `Test policy: ${opts.role} ${opts.effect} ${opts.module}:${opts.action}`,
    isActive: true,
    createdBy: 'test',
  });
}
```

- [ ] **Step 3: Verify compilation**

Run: `cd backend && npx tsc --noEmit`

Expected: zero errors.

- [ ] **Step 4: Commit**

```bash
git add backend/src/__e2e__/factories/student.factory.ts backend/src/__e2e__/factories/policy.factory.ts
git commit -m "test: add student and policy factories for E2E tests"
```

---

### Task 7: Domain factories — finance, academic, HR

**Files:**
- Create: `backend/src/__e2e__/factories/finance.factory.ts`
- Create: `backend/src/__e2e__/factories/academic.factory.ts`
- Create: `backend/src/__e2e__/factories/hr.factory.ts`

**Context:**
- FeeStructure: `collegeId, academicYearId, programmeId, branchId?, year, components: [{name, amount, isRefundable}], totalAmount`
- FeeLineItem: `collegeId, studentId, feeStructureId?, component, academicYearId, amount, paidAmount, waivedAmount, dueDate?, status`
- Payment: `collegeId, studentId, receiptNumber, amount, paymentMode, paymentDate, allocations: [{lineItemId, amount}], status`
- Course: `collegeId, code, name, regulationId, departmentId, credits, lectureHrs, tutorialHrs, practicalHrs, type, isElective`
- CourseOffering: `collegeId, courseId, semesterId, sectionId, facultyId, maxEnrollment, enrolledCount`
- Enrollment: `collegeId, studentId, courseOfferingId, semesterId, status, enrolledAt`
- Employee: `collegeId, personId, employeeId, departmentId, designation, employeeType, joiningDate, status`
- LeaveType: `collegeId, name, code, maxDaysPerYear, isCarryForward, maxCarryForward, applicableTo`
- Faculty: `collegeId, personId, employeeCode, designation, departmentId?, contractType, status`

- [ ] **Step 1: Create `backend/src/__e2e__/factories/finance.factory.ts`**

```typescript
import { FeeStructure, FeeLineItem, Payment } from '../../models';

let paymentCounter = 0;

export async function createTestFeeStructure(collegeId: string, opts: {
  academicYearId: string;
  programmeId: string;
  branchId?: string;
  year?: number;
  components?: { name: string; amount: number; isRefundable: boolean }[];
}) {
  const components = opts.components ?? [
    { name: 'Tuition', amount: 50000, isRefundable: false },
    { name: 'Lab Fee', amount: 10000, isRefundable: false },
  ];
  const totalAmount = components.reduce((sum, c) => sum + c.amount, 0);

  return FeeStructure.create({
    collegeId,
    academicYearId: opts.academicYearId,
    programmeId: opts.programmeId,
    branchId: opts.branchId,
    year: opts.year ?? 1,
    components,
    totalAmount,
  });
}

export async function createTestFeeLineItem(collegeId: string, opts: {
  studentId: string;
  academicYearId: string;
  component: string;
  amount: number;
  feeStructureId?: string;
  dueDate?: string;
}) {
  return FeeLineItem.create({
    collegeId,
    studentId: opts.studentId,
    feeStructureId: opts.feeStructureId,
    component: opts.component,
    academicYearId: opts.academicYearId,
    amount: opts.amount,
    paidAmount: 0,
    waivedAmount: 0,
    dueDate: opts.dueDate ? new Date(opts.dueDate) : undefined,
    status: 'pending',
  });
}

export async function createTestPayment(collegeId: string, opts: {
  studentId: string;
  amount: number;
  lineItemIds?: string[];
  paymentMode?: string;
}) {
  paymentCounter++;
  const allocations = (opts.lineItemIds ?? []).map((id) => ({
    lineItemId: id,
    amount: opts.amount,
  }));

  return Payment.create({
    collegeId,
    studentId: opts.studentId,
    receiptNumber: `RCP-TEST-${String(paymentCounter).padStart(6, '0')}`,
    amount: opts.amount,
    paymentMode: opts.paymentMode ?? 'cash',
    paymentDate: new Date(),
    allocations,
    status: 'success',
  });
}
```

- [ ] **Step 2: Create `backend/src/__e2e__/factories/academic.factory.ts`**

```typescript
import {
  Course, CourseOffering, Enrollment,
  AttendanceSession, InternalAssessment,
} from '../../models';
import { Person, Faculty } from '../../models';
import { createTestUser } from './user.factory';

let courseCounter = 0;
let facultyCounter = 0;

export async function createTestCourse(collegeId: string, opts: {
  regulationId: string;
  departmentId: string;
  code?: string;
  name?: string;
  credits?: number;
}) {
  courseCounter++;
  return Course.create({
    collegeId,
    code: opts.code ?? `CS${String(100 + courseCounter)}`,
    name: opts.name ?? `Test Course ${courseCounter}`,
    regulationId: opts.regulationId,
    departmentId: opts.departmentId,
    credits: opts.credits ?? 4,
    lectureHrs: 3,
    tutorialHrs: 1,
    practicalHrs: 0,
    type: 'theory',
    isElective: false,
  });
}

export async function createTestFaculty(collegeId: string, opts?: {
  departmentId?: string;
  name?: string;
  designation?: string;
}) {
  facultyCounter++;
  const name = opts?.name ?? `Test Faculty ${facultyCounter}`;
  const email = `faculty${facultyCounter}@test.com`;

  const person = await Person.create({
    collegeId,
    name,
    phone: `80000${String(facultyCounter).padStart(5, '0')}`,
    email,
    gender: 'male',
  });

  const faculty = await Faculty.create({
    collegeId,
    personId: person._id,
    employeeCode: `FAC${String(facultyCounter).padStart(4, '0')}`,
    designation: opts?.designation ?? 'Assistant Professor',
    departmentId: opts?.departmentId,
    contractType: 'regular',
    status: 'active',
  });

  const { user, token } = await createTestUser({
    collegeId,
    role: 'faculty',
    personaType: 'L-FAC',
    name,
    email,
    personId: String(person._id),
  });

  return { person, faculty, user, token };
}

export async function createTestCourseOffering(collegeId: string, opts: {
  courseId: string;
  semesterId: string;
  sectionId: string;
  facultyId: string;
  maxEnrollment?: number;
}) {
  return CourseOffering.create({
    collegeId,
    courseId: opts.courseId,
    semesterId: opts.semesterId,
    sectionId: opts.sectionId,
    facultyId: opts.facultyId,
    maxEnrollment: opts.maxEnrollment ?? 60,
    enrolledCount: 0,
  });
}

export async function createTestEnrollment(collegeId: string, opts: {
  studentId: string;
  courseOfferingId: string;
  semesterId: string;
}) {
  return Enrollment.create({
    collegeId,
    studentId: opts.studentId,
    courseOfferingId: opts.courseOfferingId,
    semesterId: opts.semesterId,
    status: 'enrolled',
    enrolledAt: new Date(),
  });
}

export async function createTestAttendanceSession(collegeId: string, opts: {
  courseOfferingId: string;
  facultyId: string;
  date?: Date;
  period?: number;
}) {
  return AttendanceSession.create({
    collegeId,
    courseOfferingId: opts.courseOfferingId,
    date: opts.date ?? new Date(),
    period: opts.period ?? 1,
    facultyId: opts.facultyId,
    status: 'open',
  });
}

export async function createTestInternalAssessment(collegeId: string, opts: {
  courseOfferingId: string;
  name?: string;
  type?: string;
  maxMarks?: number;
}) {
  return InternalAssessment.create({
    collegeId,
    courseOfferingId: opts.courseOfferingId,
    name: opts.name ?? 'Mid 1',
    type: opts.type ?? 'mid1',
    maxMarks: opts.maxMarks ?? 30,
    weightage: 15,
    status: 'conducted',
  });
}
```

- [ ] **Step 3: Create `backend/src/__e2e__/factories/hr.factory.ts`**

```typescript
import { Person, Employee } from '../../models';
import { LeaveType } from '../../models';
import { createTestUser } from './user.factory';

let empCounter = 0;
let leaveTypeCounter = 0;

export async function createTestEmployee(collegeId: string, opts?: {
  departmentId?: string;
  designation?: string;
  name?: string;
  employeeType?: string;
}) {
  empCounter++;
  const name = opts?.name ?? `Test Employee ${empCounter}`;
  const email = `emp${empCounter}@test.com`;

  const person = await Person.create({
    collegeId,
    name,
    phone: `70000${String(empCounter).padStart(5, '0')}`,
    email,
    gender: 'male',
  });

  const employee = await Employee.create({
    collegeId,
    personId: person._id,
    employeeId: `EMP${String(empCounter).padStart(4, '0')}`,
    departmentId: opts?.departmentId,
    designation: opts?.designation ?? 'Office Assistant',
    employeeType: opts?.employeeType ?? 'non_teaching',
    joiningDate: new Date('2020-06-01'),
    status: 'active',
  });

  const { user, token } = await createTestUser({
    collegeId,
    role: 'staff',
    personaType: 'ST-ADM',
    name,
    email,
    personId: String(person._id),
  });

  return { person, employee, user, token };
}

export async function createTestLeaveType(collegeId: string, opts?: {
  name?: string;
  code?: string;
  maxDaysPerYear?: number;
}) {
  leaveTypeCounter++;
  return LeaveType.create({
    collegeId,
    name: opts?.name ?? `Test Leave ${leaveTypeCounter}`,
    code: opts?.code ?? `TL${leaveTypeCounter}`,
    maxDaysPerYear: opts?.maxDaysPerYear ?? 12,
    isCarryForward: false,
    maxCarryForward: 0,
    applicableTo: ['all'],
  });
}
```

- [ ] **Step 4: Verify compilation**

Run: `cd backend && npx tsc --noEmit`

Expected: zero errors.

- [ ] **Step 5: Commit**

```bash
git add backend/src/__e2e__/factories/finance.factory.ts backend/src/__e2e__/factories/academic.factory.ts backend/src/__e2e__/factories/hr.factory.ts
git commit -m "test: add finance, academic, and HR factories for E2E tests"
```

---

### Task 8: Auth module tests

**Files:**
- Create: `backend/src/__e2e__/modules/auth.test.ts`

**Context:**
- Auth routes: `GET /api/health` (no auth), `POST /api/auth/login` (body: `{email, password, collegeId?}`), `GET /api/auth/me` (auth), `POST /api/auth/refresh` (auth)
- Login returns: `{ token, user, permissions, collegeId? }`
- Health returns: `{ status: 'ok', version: '2.0.0' }`
- `/me` returns user profile
- `/refresh` returns `{ token, permissions }`

- [ ] **Step 1: Create `backend/src/__e2e__/modules/auth.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('GET /api/health', () => {
  it('returns status ok', async () => {
    const res = await api.get('/api/health').expect(200);
    expect(res.body.status).toBe('ok');
    expect(res.body.version).toBe('2.0.0');
  });
});

describe('POST /api/auth/login', () => {
  it('returns token and permissions for valid admin credentials', async () => {
    const res = await api
      .post('/api/auth/login')
      .send({ email: 'admin@test.com', password: 'test123' })
      .expect(200);

    expect(res.body).toHaveProperty('token');
    expect(res.body).toHaveProperty('user');
    expect(res.body).toHaveProperty('permissions');
    expect(res.body.user.role).toBe('admin');
    expect(Array.isArray(res.body.permissions)).toBe(true);
    expect(res.body.permissions.length).toBeGreaterThan(0);
  });

  it('returns 401 for wrong password', async () => {
    const res = await api
      .post('/api/auth/login')
      .send({ email: 'admin@test.com', password: 'wrong-password' })
      .expect(401);

    expect(res.body).toHaveProperty('error');
  });

  it('returns 401 for non-existent email', async () => {
    const res = await api
      .post('/api/auth/login')
      .send({ email: 'nobody@test.com', password: 'test123' })
      .expect(401);

    expect(res.body).toHaveProperty('error');
  });
});

describe('GET /api/auth/me', () => {
  it('returns user profile with valid token', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/auth/me')
      .expect(200);

    expect(res.body.email).toBe('admin@test.com');
    expect(res.body.role).toBe('admin');
  });

  it('returns 401 without token', async () => {
    await api.get('/api/auth/me').expect(401);
  });

  it('returns 401 with invalid token', async () => {
    await api
      .as('invalid-token-value')
      .get('/api/auth/me')
      .expect(401);
  });
});

describe('POST /api/auth/refresh', () => {
  it('returns new token and permissions', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/auth/refresh')
      .expect(200);

    expect(res.body).toHaveProperty('token');
    expect(res.body).toHaveProperty('permissions');
    expect(typeof res.body.token).toBe('string');
  });
});
```

- [ ] **Step 2: Run the test**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/modules/auth.test.ts
```

Expected: all tests pass. If any fail, debug and fix before proceeding.

- [ ] **Step 3: Commit**

```bash
git add backend/src/__e2e__/modules/auth.test.ts
git commit -m "test: add auth module E2E tests

Tests health check, login (valid + invalid), /me endpoint,
token refresh, and 401 error scenarios."
```

---

### Task 9: Admissions module tests

**Files:**
- Create: `backend/src/__e2e__/modules/admissions.test.ts`

**Context:**
- Routes: `POST /api/admissions/inquiries`, `GET /api/admissions/inquiries`, `GET /api/admissions/inquiries/:id`, `PUT /api/admissions/inquiries/:id`, `DELETE /api/admissions/inquiries/:id`
- Routes: `POST /api/admissions/applicants`, `GET /api/admissions/applicants`, `GET /api/admissions/applicants/:id`, etc.
- createInquirySchema: `name` (required), `phone` (min 10), `source` (enum), `status` (enum)
- createApplicantSchema: `applicationNumber` (required), `name` (required), `phone` (required), `quota` (required, enum)

- [ ] **Step 1: Create `backend/src/__e2e__/modules/admissions.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { expectPaginated, expectError } from '../helpers/assertions';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Inquiries CRUD', () => {
  let inquiryId: string;

  it('POST /api/admissions/inquiries — creates inquiry', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/admissions/inquiries')
      .send({
        name: 'Rahul Kumar',
        phone: '9876543210',
        source: 'website',
        status: 'new',
        programmeInterest: 'B.Tech',
        branchInterest: 'CSE',
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.name).toBe('Rahul Kumar');
    inquiryId = res.body._id;
  });

  it('GET /api/admissions/inquiries — returns paginated list', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/admissions/inquiries')
      .expect(200);

    expectPaginated(res.body, { minItems: 1 });
  });

  it('GET /api/admissions/inquiries/:id — returns single inquiry', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/admissions/inquiries/${inquiryId}`)
      .expect(200);

    expect(res.body._id).toBe(inquiryId);
    expect(res.body.name).toBe('Rahul Kumar');
  });

  it('PUT /api/admissions/inquiries/:id — updates inquiry', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .put(`/api/admissions/inquiries/${inquiryId}`)
      .send({ status: 'contacted', notes: 'Called and interested' })
      .expect(200);

    expect(res.body.status).toBe('contacted');
  });

  it('returns 401 without auth token', async () => {
    await api.get('/api/admissions/inquiries').expect(401);
  });
});

describe('Applicants CRUD', () => {
  let applicantId: string;

  it('POST /api/admissions/applicants — creates applicant', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/admissions/applicants')
      .send({
        applicationNumber: 'APP-2024-001',
        name: 'Priya Sharma',
        phone: '9876543211',
        quota: 'management',
        status: 'submitted',
        programmeApplied: 'B.Tech',
        branchPreference1: 'CSE',
        tenthPercentage: 92,
        interPercentage: 88,
        interStream: 'MPC',
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.applicationNumber).toBe('APP-2024-001');
    applicantId = res.body._id;
  });

  it('GET /api/admissions/applicants — returns paginated list', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/admissions/applicants')
      .expect(200);

    expectPaginated(res.body, { minItems: 1 });
  });

  it('GET /api/admissions/applicants/:id — returns single applicant', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/admissions/applicants/${applicantId}`)
      .expect(200);

    expect(res.body._id).toBe(applicantId);
  });

  it('POST /api/admissions/applicants — 400 for missing required fields', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/admissions/applicants')
      .send({ name: 'No Quota' })
      .expect(400);

    expectError(res.body);
  });

  it('DELETE /api/admissions/applicants/:id — deletes applicant', async () => {
    await api
      .as(fixtures.admin.token)
      .delete(`/api/admissions/applicants/${applicantId}`)
      .expect(200);

    await api
      .as(fixtures.admin.token)
      .get(`/api/admissions/applicants/${applicantId}`)
      .expect(404);
  });
});
```

- [ ] **Step 2: Run the test**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/modules/admissions.test.ts
```

Expected: all tests pass.

- [ ] **Step 3: Commit**

```bash
git add backend/src/__e2e__/modules/admissions.test.ts
git commit -m "test: add admissions module E2E tests (inquiries + applicants CRUD)"
```

---

### Task 10: People + Finance module tests

**Files:**
- Create: `backend/src/__e2e__/modules/people.test.ts`
- Create: `backend/src/__e2e__/modules/finance.test.ts`

**Context:**
- People routes: `/api/people/students`, `/api/people/persons`, etc.
- Finance routes: `/api/finance/fee-structures`, `/api/finance/fee-line-items`, `/api/finance/payments`
- Student creation via API needs: `personId, admissionYear, status, onboardingStatus`
- FeeStructure needs: `academicYearId, programmeId, year, components, totalAmount`
- Payment needs: `studentId, amount, paymentMode, receiptNumber?`

- [ ] **Step 1: Create `backend/src/__e2e__/modules/people.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { expectPaginated } from '../helpers/assertions';
import { createTestStudent } from '../factories/student.factory';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Students CRUD', () => {
  let studentId: string;

  it('creates a student via factory and fetches via API', async () => {
    const { student } = await createTestStudent(fixtures.collegeId, {
      branchId: String(fixtures.cseBranch._id),
      batchId: String(fixtures.batch._id),
      programmeId: String(fixtures.btech._id),
    });
    studentId = String(student._id);

    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/people/students/${studentId}`)
      .expect(200);

    expect(res.body._id).toBe(studentId);
    expect(res.body.status).toBe('active');
  });

  it('GET /api/people/students — returns paginated list', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/people/students')
      .expect(200);

    expectPaginated(res.body, { minItems: 1 });
  });

  it('PUT /api/people/students/:id — updates student', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .put(`/api/people/students/${studentId}`)
      .send({ status: 'year_back' })
      .expect(200);

    expect(res.body.status).toBe('year_back');
  });

  it('returns 401 without auth token', async () => {
    await api.get('/api/people/students').expect(401);
  });

  it('returns 404 for non-existent student', async () => {
    await api
      .as(fixtures.admin.token)
      .get('/api/people/students/000000000000000000000099')
      .expect(404);
  });
});
```

- [ ] **Step 2: Create `backend/src/__e2e__/modules/finance.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { expectPaginated } from '../helpers/assertions';
import { createTestStudent } from '../factories/student.factory';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Fee Structures CRUD', () => {
  let feeStructureId: string;

  it('POST /api/finance/fee-structures — creates fee structure', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/finance/fee-structures')
      .send({
        academicYearId: String(fixtures.ay._id),
        programmeId: String(fixtures.btech._id),
        branchId: String(fixtures.cseBranch._id),
        year: 1,
        components: [
          { name: 'Tuition', amount: 50000, isRefundable: false },
          { name: 'Lab Fee', amount: 10000, isRefundable: false },
        ],
        totalAmount: 60000,
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.totalAmount).toBe(60000);
    feeStructureId = res.body._id;
  });

  it('GET /api/finance/fee-structures — returns paginated list', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/finance/fee-structures')
      .expect(200);

    expectPaginated(res.body, { minItems: 1 });
  });

  it('GET /api/finance/fee-structures/:id — returns single fee structure', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/finance/fee-structures/${feeStructureId}`)
      .expect(200);

    expect(res.body.components).toHaveLength(2);
  });
});

describe('Payments CRUD', () => {
  it('POST /api/finance/payments — creates payment', async () => {
    const { student } = await createTestStudent(fixtures.collegeId, {
      branchId: String(fixtures.cseBranch._id),
    });

    const res = await api
      .as(fixtures.admin.token)
      .post('/api/finance/payments')
      .send({
        studentId: String(student._id),
        amount: 30000,
        paymentMode: 'cash',
        paymentDate: new Date().toISOString(),
        status: 'success',
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.amount).toBe(30000);
    expect(res.body).toHaveProperty('receiptNumber');
  });

  it('GET /api/finance/payments — returns paginated list', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/finance/payments')
      .expect(200);

    expectPaginated(res.body, { minItems: 1 });
  });
});
```

- [ ] **Step 3: Run both tests**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/modules/people.test.ts src/__e2e__/modules/finance.test.ts
```

Expected: all tests pass.

- [ ] **Step 4: Commit**

```bash
git add backend/src/__e2e__/modules/people.test.ts backend/src/__e2e__/modules/finance.test.ts
git commit -m "test: add people and finance module E2E tests"
```

---

### Task 11: Academics + HR module tests

**Files:**
- Create: `backend/src/__e2e__/modules/academics.test.ts`
- Create: `backend/src/__e2e__/modules/hr.test.ts`

**Context:**
- Academics routes: `/api/academics/courses`, `/api/academics/course-offerings`, `/api/academics/enrollments`
- HR routes: `/api/hr/leave-types`, `/api/hr/leave-applications`, `/api/hr/employees`
- createCourseSchema: `code, name, regulationId, departmentId, credits, type` (all required)
- createLeaveApplicationSchema: `employeeId, leaveTypeId, fromDate, toDate, days, reason` (all required)

- [ ] **Step 1: Create `backend/src/__e2e__/modules/academics.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { expectPaginated } from '../helpers/assertions';
import { createTestFaculty, createTestCourse, createTestCourseOffering } from '../factories/academic.factory';
import { createTestStudent } from '../factories/student.factory';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Courses CRUD', () => {
  let courseId: string;

  it('POST /api/academics/courses — creates course', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/academics/courses')
      .send({
        code: 'CS201',
        name: 'Data Structures',
        regulationId: String(fixtures.regulation._id),
        departmentId: String(fixtures.cse._id),
        credits: 4,
        lectureHrs: 3,
        tutorialHrs: 1,
        practicalHrs: 0,
        type: 'theory',
        isElective: false,
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.code).toBe('CS201');
    courseId = res.body._id;
  });

  it('GET /api/academics/courses — returns paginated list', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/academics/courses')
      .expect(200);

    expectPaginated(res.body, { minItems: 1 });
  });

  it('GET /api/academics/courses/:id — returns single course', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/academics/courses/${courseId}`)
      .expect(200);

    expect(res.body.name).toBe('Data Structures');
    expect(res.body.credits).toBe(4);
  });
});

describe('Course Offerings CRUD', () => {
  it('POST /api/academics/course-offerings — creates offering', async () => {
    const { faculty } = await createTestFaculty(fixtures.collegeId, {
      departmentId: String(fixtures.cse._id),
    });
    const course = await createTestCourse(fixtures.collegeId, {
      regulationId: String(fixtures.regulation._id),
      departmentId: String(fixtures.cse._id),
    });

    const res = await api
      .as(fixtures.admin.token)
      .post('/api/academics/course-offerings')
      .send({
        courseId: String(course._id),
        semesterId: String(fixtures.sem1._id),
        sectionId: String(fixtures.cseSection._id),
        facultyId: String(faculty._id),
        maxEnrollment: 60,
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.enrolledCount).toBe(0);
  });
});
```

- [ ] **Step 2: Create `backend/src/__e2e__/modules/hr.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { expectPaginated, expectError } from '../helpers/assertions';
import { createTestEmployee, createTestLeaveType } from '../factories/hr.factory';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Leave Types CRUD', () => {
  let leaveTypeId: string;

  it('POST /api/hr/leave-types — creates leave type', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/hr/leave-types')
      .send({
        name: 'Casual Leave',
        code: 'CL',
        maxDaysPerYear: 12,
        isCarryForward: false,
        maxCarryForward: 0,
        applicableTo: ['all'],
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.name).toBe('Casual Leave');
    leaveTypeId = res.body._id;
  });

  it('GET /api/hr/leave-types — returns paginated list', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/hr/leave-types')
      .expect(200);

    expectPaginated(res.body, { minItems: 1 });
  });
});

describe('Leave Applications CRUD', () => {
  it('POST /api/hr/leave-applications — creates leave application', async () => {
    const { employee } = await createTestEmployee(fixtures.collegeId, {
      departmentId: String(fixtures.cse._id),
    });
    const leaveType = await createTestLeaveType(fixtures.collegeId, {
      name: 'Sick Leave',
      code: 'SL',
      maxDaysPerYear: 10,
    });

    const res = await api
      .as(fixtures.admin.token)
      .post('/api/hr/leave-applications')
      .send({
        employeeId: String(employee._id),
        leaveTypeId: String(leaveType._id),
        fromDate: '2025-01-10',
        toDate: '2025-01-12',
        days: 3,
        reason: 'Feeling unwell',
        status: 'applied',
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.days).toBe(3);
    expect(res.body.status).toBe('applied');
  });

  it('POST /api/hr/leave-applications — 400 for missing required fields', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/hr/leave-applications')
      .send({ reason: 'No employee or leave type' })
      .expect(400);

    expectError(res.body);
  });
});
```

- [ ] **Step 3: Run both tests**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/modules/academics.test.ts src/__e2e__/modules/hr.test.ts
```

Expected: all tests pass.

- [ ] **Step 4: Commit**

```bash
git add backend/src/__e2e__/modules/academics.test.ts backend/src/__e2e__/modules/hr.test.ts
git commit -m "test: add academics and HR module E2E tests"
```

---

### Task 12: Welfare + Platform module tests

**Files:**
- Create: `backend/src/__e2e__/modules/welfare.test.ts`
- Create: `backend/src/__e2e__/modules/platform.test.ts`

**Context:**
- Welfare routes: `/api/welfare/student-grievances`
- createStudentGrievanceSchema: `studentId, category (enum), subject, description, priority?`
- Platform routes: `/api/platform/announcements`, `/api/platform/rbac-policies`
- createAnnouncementSchema: `title, content, category (enum), postedBy, targetAudience (enum)`
- createRbacPolicySchema: `role, module, action, effect, priority`

- [ ] **Step 1: Create `backend/src/__e2e__/modules/welfare.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { expectPaginated } from '../helpers/assertions';
import { createTestStudent } from '../factories/student.factory';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Student Grievances CRUD', () => {
  let grievanceId: string;

  it('POST /api/welfare/student-grievances — creates grievance', async () => {
    const { student } = await createTestStudent(fixtures.collegeId, {
      branchId: String(fixtures.cseBranch._id),
    });

    const res = await api
      .as(fixtures.admin.token)
      .post('/api/welfare/student-grievances')
      .send({
        studentId: String(student._id),
        category: 'academic',
        subject: 'Issue with grading',
        description: 'The mid-term marks do not reflect actual performance.',
        priority: 'medium',
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.status).toBe('open');
    grievanceId = res.body._id;
  });

  it('GET /api/welfare/student-grievances — returns paginated list', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/welfare/student-grievances')
      .expect(200);

    expectPaginated(res.body, { minItems: 1 });
  });

  it('PUT /api/welfare/student-grievances/:id — updates grievance status', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .put(`/api/welfare/student-grievances/${grievanceId}`)
      .send({ status: 'in_progress' })
      .expect(200);

    expect(res.body.status).toBe('in_progress');
  });
});
```

- [ ] **Step 2: Create `backend/src/__e2e__/modules/platform.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { expectPaginated } from '../helpers/assertions';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Announcements CRUD', () => {
  let announcementId: string;

  it('POST /api/platform/announcements — creates announcement', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/platform/announcements')
      .send({
        title: 'Semester Exam Schedule',
        content: 'Exams start from March 1st. Check your hall tickets.',
        category: 'exam',
        priority: 'high',
        postedBy: String(fixtures.admin.user._id),
        targetAudience: 'students',
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.title).toBe('Semester Exam Schedule');
    announcementId = res.body._id;
  });

  it('GET /api/platform/announcements — returns paginated list', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/platform/announcements')
      .expect(200);

    expectPaginated(res.body, { minItems: 1 });
  });

  it('DELETE /api/platform/announcements/:id — deletes announcement', async () => {
    await api
      .as(fixtures.admin.token)
      .delete(`/api/platform/announcements/${announcementId}`)
      .expect(200);
  });
});

describe('RBAC Policies CRUD', () => {
  let policyId: string;

  it('GET /api/platform/rbac-policies — lists default + college policies', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/platform/rbac-policies')
      .expect(200);

    expectPaginated(res.body);
    // Should include the 69 default policies seeded in base
    expect(res.body.total).toBeGreaterThanOrEqual(10);
  });

  it('POST /api/platform/rbac-policies — creates college-specific policy', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/platform/rbac-policies')
      .send({
        role: 'student',
        module: 'hr',
        action: 'read',
        effect: 'allow',
        priority: 650,
        description: 'Allow students to read HR data for testing',
        isActive: true,
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.role).toBe('student');
    policyId = res.body._id;
  });

  it('DELETE /api/platform/rbac-policies/:id — deletes college-specific policy', async () => {
    await api
      .as(fixtures.admin.token)
      .delete(`/api/platform/rbac-policies/${policyId}`)
      .expect(200);
  });
});
```

- [ ] **Step 3: Run both tests**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/modules/welfare.test.ts src/__e2e__/modules/platform.test.ts
```

Expected: all tests pass.

- [ ] **Step 4: Commit**

```bash
git add backend/src/__e2e__/modules/welfare.test.ts backend/src/__e2e__/modules/platform.test.ts
git commit -m "test: add welfare and platform module E2E tests"
```

---

### Task 13: Workflow 01 — Auth & RBAC

**Files:**
- Create: `backend/src/__e2e__/workflows/01-auth-rbac.test.ts`

**Context:**
- This workflow tests the full auth flow + RBAC enforcement when `RBAC_ENFORCE=true`
- Default policies: admin gets `*:*`, student gets self-scoped reads on limited modules, no HR access
- The rate limiter on `/api/auth/login` allows 10 requests per 15-min window
- Tests run sequentially within the describe (ordered steps)

- [ ] **Step 1: Create `backend/src/__e2e__/workflows/01-auth-rbac.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { createTestStudent } from '../factories/student.factory';

let api: TestApi;
let fixtures: BaseFixtures;
let adminToken: string;
let studentToken: string;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();

  // Create a student for RBAC tests
  const { token } = await createTestStudent(fixtures.collegeId, {
    branchId: String(fixtures.cseBranch._id),
  });
  studentToken = token;
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Auth & RBAC Workflow', () => {
  it('Step 1: admin login returns token + permissions', async () => {
    const res = await api
      .post('/api/auth/login')
      .send({ email: 'admin@test.com', password: 'test123' })
      .expect(200);

    expect(res.body).toHaveProperty('token');
    expect(res.body).toHaveProperty('permissions');
    expect(res.body.permissions.length).toBeGreaterThan(0);
    adminToken = res.body.token;
  });

  it('Step 2: wrong password returns 401', async () => {
    await api
      .post('/api/auth/login')
      .send({ email: 'admin@test.com', password: 'wrong' })
      .expect(401);
  });

  it('Step 3: GET /me with valid token returns profile', async () => {
    const res = await api
      .as(adminToken)
      .get('/api/auth/me')
      .expect(200);

    expect(res.body.email).toBe('admin@test.com');
    expect(res.body.role).toBe('admin');
  });

  it('Step 4: admin can access finance (RBAC pass-through)', async () => {
    await api
      .as(fixtures.admin.token)
      .get('/api/finance/fee-structures')
      .expect(200);
  });

  it('Step 5: with RBAC_ENFORCE=true, student gets self-scoped finance access', async () => {
    const originalEnforce = process.env.RBAC_ENFORCE;
    process.env.RBAC_ENFORCE = 'true';

    try {
      // Student CAN read finance (self-scoped)
      const res = await api
        .as(studentToken)
        .get('/api/finance/fee-structures')
        .expect(200);

      // Should get results (may be empty, but 200 means access granted)
      expect(res.body).toHaveProperty('items');
    } finally {
      process.env.RBAC_ENFORCE = originalEnforce;
    }
  });

  it('Step 6: with RBAC_ENFORCE=true, student is denied HR access', async () => {
    const originalEnforce = process.env.RBAC_ENFORCE;
    process.env.RBAC_ENFORCE = 'true';

    try {
      await api
        .as(studentToken)
        .get('/api/hr/employees')
        .expect(403);
    } finally {
      process.env.RBAC_ENFORCE = originalEnforce;
    }
  });

  it('Step 7: token refresh returns new token + permissions', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/auth/refresh')
      .expect(200);

    expect(res.body).toHaveProperty('token');
    expect(res.body).toHaveProperty('permissions');
    expect(typeof res.body.token).toBe('string');
    expect(res.body.token).not.toBe(fixtures.admin.token);
  });

  it('Step 8: rate limiter triggers after too many bad login attempts', async () => {
    // Make enough bad requests to exhaust the rate limit (10 per 15-min window).
    // Earlier tests in this file may have consumed some of the allowance.
    // Send 10 attempts — the last ones should hit 429.
    const results: number[] = [];
    for (let i = 0; i < 10; i++) {
      const res = await api
        .post('/api/auth/login')
        .send({ email: 'ratelimit@test.com', password: 'bad' });
      results.push(res.status);
    }

    // At least one of the final attempts should be rate-limited
    expect(results).toContain(429);
  });
});
```

- [ ] **Step 2: Run the test**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/workflows/01-auth-rbac.test.ts
```

Expected: all 8 steps pass. The rate limit test may need adjustment depending on how many prior login requests were made — adjust the loop count if needed.

- [ ] **Step 3: Commit**

```bash
git add backend/src/__e2e__/workflows/01-auth-rbac.test.ts
git commit -m "test: add auth & RBAC workflow E2E test

8-step flow: login, bad password, /me, admin finance access,
student self-scope, student HR deny, token refresh, rate limiting."
```

---

### Task 14: Workflow 02 — Student Admission

**Files:**
- Create: `backend/src/__e2e__/workflows/02-student-admission.test.ts`

**Context:**
- Admission flow: Inquiry → Applicant → ExamScore → Offer → Accept → Student creation
- Inquiry routes: `POST /api/admissions/inquiries`
- Applicant routes: `POST /api/admissions/applicants`
- ExamScore routes: `POST /api/admissions/exam-scores` (body: `applicantId, examType, score, year`)
- Offer routes: `POST /api/admissions/offers` (body: `applicantId, programmeId, branchId?, feeQuoted, validityDate, status`)
- Offer accept: `PUT /api/admissions/offers/:id` (body: `status: 'accepted'`)
- Student creation: `POST /api/people/students`

- [ ] **Step 1: Create `backend/src/__e2e__/workflows/02-student-admission.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { Person } from '../../models';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Student Admission Workflow', () => {
  let inquiryId: string;
  let applicantId: string;
  let offerId: string;

  it('Step 1: create inquiry', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/admissions/inquiries')
      .send({
        name: 'Anil Reddy',
        phone: '9111222333',
        source: 'walk-in',
        status: 'new',
        programmeInterest: 'B.Tech',
        branchInterest: 'CSE',
        interPercentage: 91,
        interStream: 'MPC',
      })
      .expect(201);

    inquiryId = res.body._id;
    expect(res.body.status).toBe('new');
  });

  it('Step 2: create applicant from inquiry', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/admissions/applicants')
      .send({
        inquiryId,
        applicationNumber: 'APP-2024-100',
        name: 'Anil Reddy',
        phone: '9111222333',
        quota: 'management',
        status: 'submitted',
        programmeApplied: 'B.Tech',
        branchPreference1: 'CSE',
        interPercentage: 91,
        interStream: 'MPC',
      })
      .expect(201);

    applicantId = res.body._id;
    expect(res.body.status).toBe('submitted');
  });

  it('Step 3: add entrance exam score', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/admissions/exam-scores')
      .send({
        applicantId,
        examType: 'EAMCET',
        score: 85,
        year: 2024,
      })
      .expect(201);

    expect(res.body.examType).toBe('EAMCET');
    expect(res.body.score).toBe(85);
  });

  it('Step 4: create admission offer', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/admissions/offers')
      .send({
        applicantId,
        programmeId: String(fixtures.btech._id),
        branchId: String(fixtures.cseBranch._id),
        feeQuoted: 60000,
        validityDate: '2025-06-30',
        status: 'offered',
      })
      .expect(201);

    offerId = res.body._id;
    expect(res.body.status).toBe('offered');
  });

  it('Step 5: accept the offer', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .put(`/api/admissions/offers/${offerId}`)
      .send({ status: 'accepted' })
      .expect(200);

    expect(res.body.status).toBe('accepted');
  });

  it('Step 6: create student record', async () => {
    // First create a Person record for the student
    const person = await Person.create({
      collegeId: fixtures.collegeId,
      name: 'Anil Reddy',
      phone: '9111222333',
      email: 'anil.reddy@test.com',
      gender: 'male',
    });

    const res = await api
      .as(fixtures.admin.token)
      .post('/api/people/students')
      .send({
        personId: String(person._id),
        admissionYear: 2024,
        programmeId: String(fixtures.btech._id),
        branchId: String(fixtures.cseBranch._id),
        batchId: String(fixtures.batch._id),
        rollNumber: '24JIT0100',
        status: 'active',
        onboardingStatus: 'not_started',
      })
      .expect(201);

    expect(res.body).toHaveProperty('_id');
    expect(res.body.status).toBe('active');
    expect(res.body.rollNumber).toBe('24JIT0100');
  });

  it('Step 7: verify student record has correct links', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get('/api/people/students?rollNumber=24JIT0100')
      .expect(200);

    const items = res.body.items ?? [res.body];
    const student = items.find((s: any) => s.rollNumber === '24JIT0100');
    expect(student).toBeDefined();
    expect(String(student.branchId ?? student.branchId?._id)).toBeDefined();
  });

  it('Step 8: update applicant status to enrolled', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .put(`/api/admissions/applicants/${applicantId}`)
      .send({ status: 'enrolled' })
      .expect(200);

    expect(res.body.status).toBe('enrolled');
  });
});
```

- [ ] **Step 2: Run the test**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/workflows/02-student-admission.test.ts
```

Expected: all 8 steps pass.

- [ ] **Step 3: Commit**

```bash
git add backend/src/__e2e__/workflows/02-student-admission.test.ts
git commit -m "test: add student admission workflow E2E test

8-step flow: inquiry → applicant → exam score → offer → accept →
student creation → verify links → mark enrolled."
```

---

### Task 15: Workflow 03 — Fee Payment

**Files:**
- Create: `backend/src/__e2e__/workflows/03-fee-payment.test.ts`

**Context:**
- Fee flow: FeeStructure → FeeLineItems → Partial payment → Full payment → Verify status transitions
- FeeLineItem status transitions: pending → partial → paid
- Payment allocations: `[{ lineItemId, amount }]`
- The service should update lineItem `paidAmount` and `status` after each payment

- [ ] **Step 1: Create `backend/src/__e2e__/workflows/03-fee-payment.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { createTestStudent } from '../factories/student.factory';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Fee → Payment Workflow', () => {
  let studentId: string;
  let feeStructureId: string;
  let tuitionLineItemId: string;
  let labLineItemId: string;

  it('Step 1: create fee structure (tuition: 50000, lab: 10000)', async () => {
    const { student } = await createTestStudent(fixtures.collegeId, {
      branchId: String(fixtures.cseBranch._id),
      programmeId: String(fixtures.btech._id),
    });
    studentId = String(student._id);

    const res = await api
      .as(fixtures.admin.token)
      .post('/api/finance/fee-structures')
      .send({
        academicYearId: String(fixtures.ay._id),
        programmeId: String(fixtures.btech._id),
        branchId: String(fixtures.cseBranch._id),
        year: 1,
        components: [
          { name: 'Tuition', amount: 50000, isRefundable: false },
          { name: 'Lab Fee', amount: 10000, isRefundable: false },
        ],
        totalAmount: 60000,
      })
      .expect(201);

    feeStructureId = res.body._id;
    expect(res.body.totalAmount).toBe(60000);
  });

  it('Step 2: create fee line items for student', async () => {
    const tuitionRes = await api
      .as(fixtures.admin.token)
      .post('/api/finance/fee-line-items')
      .send({
        studentId,
        feeStructureId,
        component: 'Tuition',
        academicYearId: String(fixtures.ay._id),
        amount: 50000,
        paidAmount: 0,
        waivedAmount: 0,
        status: 'pending',
      })
      .expect(201);

    tuitionLineItemId = tuitionRes.body._id;

    const labRes = await api
      .as(fixtures.admin.token)
      .post('/api/finance/fee-line-items')
      .send({
        studentId,
        feeStructureId,
        component: 'Lab Fee',
        academicYearId: String(fixtures.ay._id),
        amount: 10000,
        paidAmount: 0,
        waivedAmount: 0,
        status: 'pending',
      })
      .expect(201);

    labLineItemId = labRes.body._id;
  });

  it('Step 3: verify both line items are pending', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/finance/fee-line-items?studentId=${studentId}`)
      .expect(200);

    const items = res.body.items;
    expect(items).toHaveLength(2);
    expect(items.every((i: any) => i.status === 'pending')).toBe(true);
  });

  it('Step 4: partial payment of 30000 against tuition', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/finance/payments')
      .send({
        studentId,
        amount: 30000,
        paymentMode: 'cash',
        paymentDate: new Date().toISOString(),
        allocations: [{ lineItemId: tuitionLineItemId, amount: 30000 }],
        status: 'success',
      })
      .expect(201);

    expect(res.body.amount).toBe(30000);
  });

  it('Step 5: verify tuition line item is partial', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/finance/fee-line-items/${tuitionLineItemId}`)
      .expect(200);

    expect(res.body.paidAmount).toBe(30000);
    expect(res.body.status).toBe('partial');
  });

  it('Step 6: remaining payment of 20000 against tuition', async () => {
    await api
      .as(fixtures.admin.token)
      .post('/api/finance/payments')
      .send({
        studentId,
        amount: 20000,
        paymentMode: 'upi',
        paymentDate: new Date().toISOString(),
        allocations: [{ lineItemId: tuitionLineItemId, amount: 20000 }],
        status: 'success',
      })
      .expect(201);
  });

  it('Step 7: verify tuition line item is paid', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/finance/fee-line-items/${tuitionLineItemId}`)
      .expect(200);

    expect(res.body.paidAmount).toBe(50000);
    expect(res.body.status).toBe('paid');
  });

  it('Step 8: pay lab fee in full', async () => {
    await api
      .as(fixtures.admin.token)
      .post('/api/finance/payments')
      .send({
        studentId,
        amount: 10000,
        paymentMode: 'card',
        paymentDate: new Date().toISOString(),
        allocations: [{ lineItemId: labLineItemId, amount: 10000 }],
        status: 'success',
      })
      .expect(201);
  });

  it('Step 9: verify lab fee line item is paid', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/finance/fee-line-items/${labLineItemId}`)
      .expect(200);

    expect(res.body.paidAmount).toBe(10000);
    expect(res.body.status).toBe('paid');
  });
});
```

- [ ] **Step 2: Run the test**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/workflows/03-fee-payment.test.ts
```

Expected: all 9 steps pass. If the service doesn't automatically update `paidAmount`/`status` on payment creation, some assertions may fail — note this as a concern for the existing finance service to handle payment allocation side effects.

- [ ] **Step 3: Commit**

```bash
git add backend/src/__e2e__/workflows/03-fee-payment.test.ts
git commit -m "test: add fee payment workflow E2E test

9-step flow: fee structure → line items → partial payment → verify partial →
full payment → verify paid → lab fee → verify all paid."
```

---

### Task 16: Workflow 04 — Academic Delivery

**Files:**
- Create: `backend/src/__e2e__/workflows/04-academic-delivery.test.ts`

**Context:**
- Academic flow: Course → Faculty → CourseOffering → Enrollment → AttendanceSession → AttendanceRecord → InternalAssessment → InternalMark
- createAttendanceRecordSchema: `sessionId, studentId, status (enum: present|absent|late|od|leave), markedBy`
- createInternalMarkSchema: `assessmentId, studentId, marksObtained`

- [ ] **Step 1: Create `backend/src/__e2e__/workflows/04-academic-delivery.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { createTestFaculty } from '../factories/academic.factory';
import { createTestStudent } from '../factories/student.factory';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Academic Delivery Workflow', () => {
  let courseId: string;
  let facultyId: string;
  let facultyToken: string;
  let courseOfferingId: string;
  let studentId: string;
  let sessionId: string;
  let assessmentId: string;

  it('Step 1: create course (Data Structures, 4 credits)', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/academics/courses')
      .send({
        code: 'CS202',
        name: 'Data Structures',
        regulationId: String(fixtures.regulation._id),
        departmentId: String(fixtures.cse._id),
        credits: 4,
        lectureHrs: 3,
        tutorialHrs: 1,
        practicalHrs: 0,
        type: 'theory',
        isElective: false,
      })
      .expect(201);

    courseId = res.body._id;
    expect(res.body.credits).toBe(4);
  });

  it('Step 2: create faculty + student via factories', async () => {
    const { faculty, token } = await createTestFaculty(fixtures.collegeId, {
      departmentId: String(fixtures.cse._id),
      name: 'Dr. Kumar',
    });
    facultyId = String(faculty._id);
    facultyToken = token;

    const { student } = await createTestStudent(fixtures.collegeId, {
      branchId: String(fixtures.cseBranch._id),
      batchId: String(fixtures.batch._id),
      programmeId: String(fixtures.btech._id),
    });
    studentId = String(student._id);
  });

  it('Step 3: create course offering', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/academics/course-offerings')
      .send({
        courseId,
        semesterId: String(fixtures.sem1._id),
        sectionId: String(fixtures.cseSection._id),
        facultyId,
        maxEnrollment: 60,
      })
      .expect(201);

    courseOfferingId = res.body._id;
    expect(res.body.enrolledCount).toBe(0);
  });

  it('Step 4: enroll student in course', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/academics/enrollments')
      .send({
        studentId,
        courseOfferingId,
        semesterId: String(fixtures.sem1._id),
        status: 'enrolled',
      })
      .expect(201);

    expect(res.body.status).toBe('enrolled');
  });

  it('Step 5: create attendance session', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/academics/attendance-sessions')
      .send({
        courseOfferingId,
        date: new Date().toISOString(),
        period: 1,
        facultyId,
        status: 'open',
      })
      .expect(201);

    sessionId = res.body._id;
  });

  it('Step 6: mark student present', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/academics/attendance-records')
      .send({
        sessionId,
        studentId,
        status: 'present',
        markedBy: facultyId,
      })
      .expect(201);

    expect(res.body.status).toBe('present');
  });

  it('Step 7: create internal assessment', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/academics/internal-assessments')
      .send({
        courseOfferingId,
        name: 'Mid Term 1',
        type: 'mid1',
        maxMarks: 30,
        weightage: 15,
        status: 'conducted',
      })
      .expect(201);

    assessmentId = res.body._id;
  });

  it('Step 8: enter internal marks for student', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/academics/internal-marks')
      .send({
        assessmentId,
        studentId,
        marksObtained: 25,
      })
      .expect(201);

    expect(res.body.marksObtained).toBe(25);
  });

  it('Step 9: verify enrollment is still correct', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/academics/enrollments?studentId=${studentId}`)
      .expect(200);

    const items = res.body.items;
    expect(items.length).toBeGreaterThanOrEqual(1);
    const enrollment = items.find((e: any) =>
      String(e.courseOfferingId?._id ?? e.courseOfferingId) === courseOfferingId
    );
    expect(enrollment).toBeDefined();
    expect(enrollment.status).toBe('enrolled');
  });
});
```

- [ ] **Step 2: Run the test**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/workflows/04-academic-delivery.test.ts
```

Expected: all 9 steps pass.

- [ ] **Step 3: Commit**

```bash
git add backend/src/__e2e__/workflows/04-academic-delivery.test.ts
git commit -m "test: add academic delivery workflow E2E test

9-step flow: course → faculty → offering → enrollment → attendance
session → mark present → internal assessment → marks → verify."
```

---

### Task 17: Workflow 05 — Leave Management

**Files:**
- Create: `backend/src/__e2e__/workflows/05-leave-management.test.ts`

**Context:**
- Leave flow: LeaveType → Employee → LeaveBalance → Apply → Approve → Verify balance deduction
- LeaveBalance: `employeeId, leaveTypeId, academicYearId, entitled, taken, balance`
- LeaveApplication status: `applied → approved → rejected → cancelled`
- The service may or may not auto-deduct balance on approval — the test verifies both approaches

- [ ] **Step 1: Create `backend/src/__e2e__/workflows/05-leave-management.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { createTestEmployee, createTestLeaveType } from '../factories/hr.factory';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
});

afterAll(async () => {
  await cleanupTestApp();
});

describe('Leave Management Workflow', () => {
  let leaveTypeId: string;
  let employeeId: string;
  let leaveBalanceId: string;
  let leaveApplicationId: string;

  it('Step 1: create leave type (Casual Leave, 12 days)', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/hr/leave-types')
      .send({
        name: 'Casual Leave',
        code: 'CL',
        maxDaysPerYear: 12,
        isCarryForward: false,
        maxCarryForward: 0,
        applicableTo: ['all'],
      })
      .expect(201);

    leaveTypeId = res.body._id;
    expect(res.body.maxDaysPerYear).toBe(12);
  });

  it('Step 2: create employee via factory', async () => {
    const { employee } = await createTestEmployee(fixtures.collegeId, {
      departmentId: String(fixtures.cse._id),
      name: 'Suresh Kumar',
    });
    employeeId = String(employee._id);
  });

  it('Step 3: create leave balance (12 days)', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/hr/leave-balances')
      .send({
        employeeId,
        leaveTypeId,
        academicYearId: String(fixtures.ay._id),
        entitled: 12,
        taken: 0,
        balance: 12,
      })
      .expect(201);

    leaveBalanceId = res.body._id;
    expect(res.body.balance).toBe(12);
  });

  it('Step 4: apply for 3 days leave', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/hr/leave-applications')
      .send({
        employeeId,
        leaveTypeId,
        fromDate: '2025-02-10',
        toDate: '2025-02-12',
        days: 3,
        reason: 'Family function',
        status: 'applied',
      })
      .expect(201);

    leaveApplicationId = res.body._id;
    expect(res.body.status).toBe('applied');
    expect(res.body.days).toBe(3);
  });

  it('Step 5: approve the leave', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .put(`/api/hr/leave-applications/${leaveApplicationId}`)
      .send({ status: 'approved', approvedBy: String(fixtures.admin.user._id) })
      .expect(200);

    expect(res.body.status).toBe('approved');
  });

  it('Step 6: verify leave balance (may be auto-deducted)', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .get(`/api/hr/leave-balances/${leaveBalanceId}`)
      .expect(200);

    // If the service auto-deducts on approval: balance = 9, taken = 3
    // If manual: balance = 12, taken = 0 (still valid — just no auto-deduction)
    // Either outcome is acceptable at this point
    expect(res.body.entitled).toBe(12);
    expect(res.body.balance).toBeGreaterThanOrEqual(0);
  });

  it('Step 7: apply for leave exceeding remaining balance', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/hr/leave-applications')
      .send({
        employeeId,
        leaveTypeId,
        fromDate: '2025-03-01',
        toDate: '2025-03-15',
        days: 15,
        reason: 'Extended leave',
        status: 'applied',
      });

    // Service may accept (and let approver decide) or reject
    // Both 201 and 400 are acceptable outcomes
    expect([201, 400]).toContain(res.status);
  });
});
```

- [ ] **Step 2: Run the test**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/workflows/05-leave-management.test.ts
```

Expected: all 7 steps pass.

- [ ] **Step 3: Commit**

```bash
git add backend/src/__e2e__/workflows/05-leave-management.test.ts
git commit -m "test: add leave management workflow E2E test

7-step flow: leave type → employee → balance → apply 3d → approve →
verify balance → test over-limit application."
```

---

### Task 18: Workflow 06 — RBAC Policy Override

**Files:**
- Create: `backend/src/__e2e__/workflows/06-rbac-policy-override.test.ts`

**Context:**
- This workflow verifies that college-specific RBAC policy overrides take effect at login and enforcement time
- Default policies deny students access to HR module (no student→hr policy exists in defaults)
- Creating a college-specific `allow student read hr` policy should grant access
- Deleting it should revoke access
- System default policies (createdBy: 'seed', no collegeId) cannot be deleted

- [ ] **Step 1: Create `backend/src/__e2e__/workflows/06-rbac-policy-override.test.ts`**

```typescript
import { getTestApp, cleanupTestApp } from '../setup/test-app';
import { seedBase, BaseFixtures } from '../setup/seed-base';
import { createTestApi, TestApi } from '../helpers/request';
import { createTestStudent } from '../factories/student.factory';
import { Policy } from '../../models/platform/Policy';

let api: TestApi;
let fixtures: BaseFixtures;

beforeAll(async () => {
  const app = await getTestApp();
  api = createTestApi(app);
  fixtures = await seedBase();
  // Enable RBAC enforcement for this entire test file
  process.env.RBAC_ENFORCE = 'true';
});

afterAll(async () => {
  process.env.RBAC_ENFORCE = 'false';
  await cleanupTestApp();
});

describe('RBAC Policy Override Workflow', () => {
  let studentEmail: string;
  let studentToken: string;
  let overridePolicyId: string;

  it('Step 1: create student and login — permissions do NOT include hr:read', async () => {
    const { token } = await createTestStudent(fixtures.collegeId, {
      branchId: String(fixtures.cseBranch._id),
      name: 'RBAC Test Student',
      email: 'rbac-student@test.com',
    });
    studentEmail = 'rbac-student@test.com';

    // Login to get server-resolved permissions
    const res = await api
      .post('/api/auth/login')
      .send({ email: studentEmail, password: 'test123' })
      .expect(200);

    studentToken = res.body.token;
    const permissions: string[] = res.body.permissions;
    expect(permissions).not.toContain('hr:read');
  });

  it('Step 2: student cannot access HR employees', async () => {
    await api
      .as(studentToken)
      .get('/api/hr/employees')
      .expect(403);
  });

  it('Step 3: admin creates college-specific policy allowing student→hr:read', async () => {
    const res = await api
      .as(fixtures.admin.token)
      .post('/api/platform/rbac-policies')
      .send({
        role: 'student',
        module: 'hr',
        action: 'read',
        effect: 'allow',
        priority: 650,
        description: 'Override: allow students to read HR',
        isActive: true,
      })
      .expect(201);

    overridePolicyId = res.body._id;
  });

  it('Step 4: student logs in again — permissions now include hr:read', async () => {
    const res = await api
      .post('/api/auth/login')
      .send({ email: studentEmail, password: 'test123' })
      .expect(200);

    studentToken = res.body.token;
    const permissions: string[] = res.body.permissions;
    expect(permissions).toContain('hr:read');
  });

  it('Step 5: student can now access HR employees', async () => {
    await api
      .as(studentToken)
      .get('/api/hr/employees')
      .expect(200);
  });

  it('Step 6: admin deletes the override policy', async () => {
    await api
      .as(fixtures.admin.token)
      .delete(`/api/platform/rbac-policies/${overridePolicyId}`)
      .expect(200);
  });

  it('Step 7: student logs in again — permissions no longer include hr:read', async () => {
    const res = await api
      .post('/api/auth/login')
      .send({ email: studentEmail, password: 'test123' })
      .expect(200);

    studentToken = res.body.token;
    const permissions: string[] = res.body.permissions;
    expect(permissions).not.toContain('hr:read');
  });

  it('Step 8: system default policies cannot be deleted', async () => {
    // Find a system default policy (no collegeId, createdBy = 'seed')
    const systemPolicy = await Policy.findOne({ createdBy: 'seed' });
    expect(systemPolicy).not.toBeNull();

    await api
      .as(fixtures.admin.token)
      .delete(`/api/platform/rbac-policies/${String(systemPolicy!._id)}`)
      .expect(403);
  });
});
```

- [ ] **Step 2: Run the test**

```bash
cd backend && npx vitest run --config vitest.e2e.config.ts src/__e2e__/workflows/06-rbac-policy-override.test.ts
```

Expected: all 8 steps pass.

- [ ] **Step 3: Run the full E2E suite**

```bash
cd backend && npm run test:e2e
```

Expected: all test files pass. Report total test count and run time.

- [ ] **Step 4: Run typecheck**

```bash
cd backend && npm run typecheck
```

Expected: zero errors.

- [ ] **Step 5: Commit**

```bash
git add backend/src/__e2e__/workflows/06-rbac-policy-override.test.ts
git commit -m "test: add RBAC policy override workflow E2E test

8-step flow: verify student denied HR → create override → verify
student gains HR access → delete override → verify access revoked →
verify system defaults protected."
```

---

## File Index

| Task | Files Created / Modified |
|------|-------------------------|
| 1 | Create `server.ts`, Modify `app.ts` + `package.json`, Create `vitest.e2e.config.ts` |
| 2 | Create `__e2e__/setup/global-setup.ts`, `global-teardown.ts`, `test-app.ts` |
| 3 | Create `__e2e__/factories/user.factory.ts` |
| 4 | Create `__e2e__/setup/seed-base.ts` |
| 5 | Create `__e2e__/helpers/request.ts`, `assertions.ts` |
| 6 | Create `__e2e__/factories/student.factory.ts`, `policy.factory.ts` |
| 7 | Create `__e2e__/factories/finance.factory.ts`, `academic.factory.ts`, `hr.factory.ts` |
| 8 | Create `__e2e__/modules/auth.test.ts` |
| 9 | Create `__e2e__/modules/admissions.test.ts` |
| 10 | Create `__e2e__/modules/people.test.ts`, `finance.test.ts` |
| 11 | Create `__e2e__/modules/academics.test.ts`, `hr.test.ts` |
| 12 | Create `__e2e__/modules/welfare.test.ts`, `platform.test.ts` |
| 13 | Create `__e2e__/workflows/01-auth-rbac.test.ts` |
| 14 | Create `__e2e__/workflows/02-student-admission.test.ts` |
| 15 | Create `__e2e__/workflows/03-fee-payment.test.ts` |
| 16 | Create `__e2e__/workflows/04-academic-delivery.test.ts` |
| 17 | Create `__e2e__/workflows/05-leave-management.test.ts` |
| 18 | Create `__e2e__/workflows/06-rbac-policy-override.test.ts` |
